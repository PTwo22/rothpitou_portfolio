// main page component after action and/or "cut-scene" in startpage (TBC)
import React from 'react';
import Header from '../components/Header';

function HomePage(){
    return (
        <Header />
    );
}

export default HomePage;